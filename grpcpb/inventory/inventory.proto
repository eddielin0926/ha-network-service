syntax = "proto3";

option go_package = "grpcb/inventory";

package inventory;

service Inventory {
    rpc Get (Order) returns (Material);
}

message Order {
    int32 a = 1;
}

message Material {
    int32 material = 1;
    string signature = 2;
}
